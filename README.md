# scripts

## generate jwt for cloud
```py
usage: generate_jwt.py [-h] [--service-file SERVICE_FILE] [--install-dependencies]

Generate JWT for Google Cloud IAP

options:
  -h, --help            show this help message and exit
  --service-file SERVICE_FILE, -sF SERVICE_FILE
                        Path to the service account file
  --install-dependencies, -I
                        Install required dependencies
```
